generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                      String           @id @default(cuid())
  clerkId                 String           @unique
  email                   String           @unique
  name                    String?
  imageUrl                String?
  role                    Role             @default(USER)
  plan                    PlanType         @default(FREE)
  planExpiresAt           DateTime?
  createdAt               DateTime         @default(now())
  updatedAt               DateTime         @updatedAt
  major                   String?
  phone                   String?
  consentAcceptedAt       DateTime?
  adminAccessLogsGiven    AdminAccessLog[] @relation("AdminAccessLogAsAdmin")
  adminAccessLogsReceived AdminAccessLog[] @relation("AdminAccessLogAsTarget")
  chatMessages            ChatMessage[]
  payments                Payment[]
  resumes                 Resume[]

  @@map("users")
}

model Resume {
  id                     String          @id @default(cuid())
  userId                 String
  title                  String          @default("My Resume")
  template               String          @default("professional")
  language               String          @default("en")
  isPublic               Boolean         @default(false)
  atsScore               Int?
  createdAt              DateTime        @default(now())
  updatedAt              DateTime        @updatedAt
  coverLetterData        String?
  coverLetterFileName    String?
  coverLetterMimeType    String?
  skillsSection          Json?
  experienceOrder        String          @default("auto")
  portfolioEnabled       Boolean         @default(false)
  portfolioSlug          String?         @unique
  portfolioTheme         String          @default("midnight")
  portfolioSectionOrder  Json?
  portfolioTemplate      String          @default("classic")
  portfolioCustomization Json?
  portfolioViewCount     Int             @default(0)
  awards                 Award[]
  certifications         Certification[]
  education              Education[]
  experiences            Experience[]
  languages              Language[]
  personalInfo           PersonalInfo?
  projects               Project[]
  user                   User            @relation(fields: [userId], references: [id], onDelete: Cascade)
  skills                 Skill[]
  softSkills             SoftSkill[]
  summary                Summary?
  volunteering           Volunteering[]

  @@index([userId])
  @@map("resumes")
}

model PersonalInfo {
  id         String  @id @default(cuid())
  resumeId   String  @unique
  fullName   String  @default("")
  title      String  @default("")
  email      String  @default("")
  phone      String  @default("")
  location   String  @default("")
  linkedin   String  @default("")
  github     String  @default("")
  website    String  @default("")
  profilePic String?
  resume     Resume  @relation(fields: [resumeId], references: [id], onDelete: Cascade)

  @@map("personal_info")
}

model Summary {
  id       String @id @default(cuid())
  resumeId String @unique
  content  String @default("")
  resume   Resume @relation(fields: [resumeId], references: [id], onDelete: Cascade)

  @@map("summaries")
}

model Skill {
  id       String     @id @default(cuid())
  resumeId String
  name     String
  level    SkillLevel @default(INTERMEDIATE)
  category String?
  order    Int        @default(0)
  resume   Resume     @relation(fields: [resumeId], references: [id], onDelete: Cascade)

  @@map("skills")
}

model SoftSkill {
  id       String @id @default(cuid())
  resumeId String
  name     String
  order    Int    @default(0)
  resume   Resume @relation(fields: [resumeId], references: [id], onDelete: Cascade)

  @@map("soft_skills")
}

model Language {
  id       String @id @default(cuid())
  resumeId String
  name     String
  level    String @default("")
  order    Int    @default(0)
  resume   Resume @relation(fields: [resumeId], references: [id], onDelete: Cascade)

  @@map("languages")
}

model Project {
  id          String   @id @default(cuid())
  resumeId    String
  name        String
  description String?
  url         String?
  github      String?
  tech        String[]
  startDate   String?
  endDate     String?
  current     Boolean  @default(false)
  order       Int      @default(0)
  resume      Resume   @relation(fields: [resumeId], references: [id], onDelete: Cascade)

  @@map("projects")
}

model Experience {
  id          String   @id @default(cuid())
  resumeId    String
  company     String
  position    String
  location    String?
  startDate   String
  endDate     String?
  current     Boolean  @default(false)
  description String[]
  order       Int      @default(0)
  resume      Resume   @relation(fields: [resumeId], references: [id], onDelete: Cascade)

  @@map("experiences")
}

model Education {
  id          String   @id @default(cuid())
  resumeId    String
  institution String
  degree      String
  field       String?
  location    String?
  startDate   String
  endDate     String?
  current     Boolean  @default(false)
  gpa         String?
  description String[]
  order       Int      @default(0)
  resume      Resume   @relation(fields: [resumeId], references: [id], onDelete: Cascade)

  @@map("education")
}

model Certification {
  id           String  @id @default(cuid())
  resumeId     String
  name         String
  issuer       String
  issueDate    String?
  expiryDate   String?
  credentialId String?
  url          String?
  order        Int     @default(0)
  resume       Resume  @relation(fields: [resumeId], references: [id], onDelete: Cascade)

  @@map("certifications")
}

model Award {
  id          String  @id @default(cuid())
  resumeId    String
  title       String
  issuer      String?
  date        String?
  description String?
  order       Int     @default(0)
  resume      Resume  @relation(fields: [resumeId], references: [id], onDelete: Cascade)

  @@map("awards")
}

model Volunteering {
  id           String   @id @default(cuid())
  resumeId     String
  organization String
  role         String
  location     String?
  startDate    String
  endDate      String?
  current      Boolean  @default(false)
  description  String[]
  order        Int      @default(0)
  resume       Resume   @relation(fields: [resumeId], references: [id], onDelete: Cascade)

  @@map("volunteering")
}

model Plan {
  id          String   @id @default(cuid())
  name        String
  type        PlanType @unique
  price       Float
  yearlyPrice Float?
  features    String[]
  maxResumes  Int      @default(1)
  hasAI       Boolean  @default(false)
  hasATS      Boolean  @default(false)
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("plans")
}

model Payment {
  id        String        @id @default(cuid())
  userId    String
  amount    Float
  currency  String        @default("USD")
  status    PaymentStatus @default(PENDING)
  provider  String
  sessionId String?
  createdAt DateTime      @default(now())
  user      User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("payments")
}

model PlatformVisit {
  id        String   @id @default(cuid())
  path      String?
  createdAt DateTime @default(now())

  @@map("platform_visits")
}

model AdminAccessLog {
  id           String          @id @default(cuid())
  adminUserId  String
  targetUserId String
  resumeId     String?
  createdAt    DateTime        @default(now())
  action       AdminActionType @default(VIEWED_RESUME)
  metadata     String?
  admin        User            @relation("AdminAccessLogAsAdmin", fields: [adminUserId], references: [id], onDelete: Cascade)
  targetUser   User            @relation("AdminAccessLogAsTarget", fields: [targetUserId], references: [id], onDelete: Cascade)

  @@index([targetUserId, createdAt])
  @@index([adminUserId, createdAt])
  @@map("admin_access_logs")
}

model ChatMessage {
  id        String   @id @default(cuid())
  userId    String
  role      ChatRole
  content   String
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, createdAt])
  @@map("chat_messages")
}

enum Role {
  USER
  ADMIN
  CAREER_ADVISOR
}

enum PlanType {
  FREE
  PREMIUM
}

enum SkillLevel {
  BEGINNER
  INTERMEDIATE
  ADVANCED
  EXPERT
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
  REFUNDED
}

enum AdminActionType {
  VIEWED_RESUME
  CHANGED_ROLE
}

enum ChatRole {
  USER
  ASSISTANT
}
